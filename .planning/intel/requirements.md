# Requirements (from PRDs)

No PRD-classified documents in this ingest set. None of the 16 classified planning docs
were product requirement documents, so no `REQ-*` entries were extracted and no competing
acceptance variants exist.

Feature intent lives in the SPEC design docs (see `constraints.md`) and the DOC plans /
launch materials (see `context.md`).
