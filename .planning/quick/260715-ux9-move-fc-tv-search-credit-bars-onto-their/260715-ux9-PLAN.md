---
phase: 260715-ux9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/claude_statusbar/progress.py
  - src/claude_statusbar/styles.py
  - tests/test_provider_usage_render.py
autonomous: true
requirements: [UX9-01]
must_haves:
  truths:
    - fc/tv search-credit bars render on their OWN row directly below the primary quota line in all three styles (classic, capsule, hairline).
    - Cache-age text stays on the primary (first) line in classic style — it is never pushed onto the fc/tv row.
    - Empty or None search_credits produces NO extra line and no trailing/blank-line artifact, in every mode (no_quota, quota_stale, normal) and every style.
    - Each style keeps its own idiom on the new row (classic = bracket batteries joined by muted " | "; capsule = pills joined by "╱"; hairline = chips joined by "┊").
    - use_color=False output on the new row carries no stray ANSI escapes/resets.
  artifacts:
    - src/claude_statusbar/progress.py  # new format_search_credit_line() helper; fc/tv removed from format_status_line's inline parts
    - src/claude_statusbar/styles.py     # render_classic/capsule/hairline emit fc/tv as a separate "\n"-joined row
    - tests/test_provider_usage_render.py # newline-placement assertions + cache-interaction guard
  key_links:
    - render_classic appends the fc/tv row AFTER the cache-age suffix — the guarantee that cache stays on line 1.
    - render_capsule/render_hairline append the credit row BEFORE their `_strip(line)` call so no-color mode stays ANSI-free.
    - core.main() and preview.py both route through styles.render → the renderer, so the row appears with no change to those callers.
---

<objective>
Move the Firecrawl (`fc`) / Tavily (`tv`) search-credit bars off the end of the primary quota line and onto their OWN status-line row, directly below the primary quota bar, in every style. Each style keeps its existing visual idiom for that row.

Purpose: The search-credit bars currently crowd the tail of the primary quota line. A dedicated row below the quota bar reads cleaner and scales when more providers are added.
Output: fc/tv render on a second line under the quota line in classic, capsule, and hairline; cache-age stays on the quota line; empty credits render nothing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/claude_statusbar/progress.py
@src/claude_statusbar/styles.py
@tests/test_provider_usage_render.py

# Callers that route through styles.render (no change needed, but read to confirm):
#   core.py ~1262-1273 builds search_kwargs={"search_credits": segs} and passes it to styles.render
#   preview.py ~227,247 call styles.render(search_credits=...) with demo/live entries
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit fc/tv as a separate row in all three style renderers</name>
  <files>src/claude_statusbar/progress.py, src/claude_statusbar/styles.py</files>
  <action>
Chosen approach (per investigation, cleanest of the two options): `format_status_line` builds ONLY the primary quota line; the fc/tv row is generated separately and appended by the style renderer AFTER the primary line (including cache-age) is complete. This keeps cache-age placement trivially correct and makes all three styles symmetric.

progress.py:
1. Remove the three `parts.extend(_search_credit_parts(search_credits, theme, use_color))` calls inside `format_status_line` — one in the no_quota branch (~line 633), one in the quota_stale branch (~line 658), and one in the normal branch (~line 738). After this, `format_status_line` never renders fc/tv inline in any branch.
2. Keep the `search_credits=None` parameter in `format_status_line`'s signature (retained for signature stability; it is now inert — the row is emitted by the style renderer). Add a one-line docstring note stating search credits now render on their own row via the style renderer.
3. Add a new module-level function `format_search_credit_line(search_credits, theme=None, use_color=True)` immediately after `_search_credit_parts`. It resolves the theme (default `get_theme("graphite")`), calls `_search_credit_parts(...)`, and returns those parts joined by the muted separator `colorize(" | ", _fg(theme.mute), use_color)` — the SAME separator the primary line uses (classic idiom). It returns an empty string for a falsy/empty parts list so callers can append unconditionally.

styles.py — render_classic (~line 398-465):
4. Import `format_search_credit_line` alongside the existing `from .progress import format_status_line, _fg, colorize, RESET`.
5. Drop the now-inert `search_credits=search_credits` kwarg from the `format_status_line(...)` call (~line 452) so data flow is honest — format_status_line no longer consumes it.
6. After the existing cache-age append block (the `if cache_age_text:` block that does `result += ... cache {cache_age_text}`, ending ~line 464) and BEFORE `return result`: compute `credit_row = format_search_credit_line(search_credits, theme, use_color)` and, when truthy, do `result += "\n" + credit_row`. Because this runs strictly after the cache suffix, cache-age is guaranteed to stay on the first (primary) line and fc/tv land on the row below.

styles.py — render_capsule (~line 119-257):
7. Remove the fc/tv pill loop `for entry in (search_credits or []): parts.append(pill(fill, entry.get("text","")))` (~lines 239-241) so fc/tv are no longer part of the primary `parts` list.
8. After `line = spacer.join(parts)` (~line 250) and the `if bypass:` append (~lines 252-253), and BEFORE `if not use_color: return _strip(line)`: build the credit pills — for each entry in `(search_credits or [])`, `pill(_balance_fill_rgb(entry.get("pct"), theme), entry.get("text",""))` — and when the list is non-empty do `line += "\n" + spacer.join(credit_pills)`, reusing the same `spacer` (`EDGE ╱ `) the primary pills use (capsule idiom). `_balance_fill_rgb` is already imported at the top of render_capsule. Placing this before `_strip(line)` ensures the row is ANSI-stripped together with the rest in no-color mode; `_strip` preserves the newline.

styles.py — render_hairline (~line 263-392):
9. Remove the fc/tv chip loop `for entry in (search_credits or []): parts.append(...)` (~lines 375-377) so fc/tv are no longer part of `parts`.
10. After `line = sep.join(parts)` (~line 389) and BEFORE `if not use_color: return _strip(line)`: build the credit chips — for each entry in `(search_credits or [])`, `f"{_fg(_balance_fill_rgb(entry.get('pct'), theme))}{entry.get('text','')}{RESET}"` — and when the list is non-empty do `line += "\n" + sep.join(credit_chips)`, reusing the same `sep` (`┊`) the primary segments use (hairline idiom). `_balance_fill_rgb` is already imported at the top of render_hairline.

Edge-case guarantees this design gives for free: empty/None search_credits → no credit parts → no `"\n"` appended → no stray blank row, in every branch/mode/style. use_color=False → classic uses `colorize` (clean text, no ANSI); capsule/hairline pass the whole multi-line string through `_strip` (removes ANSI, keeps the newline). No change to core.py or preview.py — both already route through styles.render, so the row appears automatically.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from claude_statusbar import styles; E=[{'label':'fc','pct':82.0,'text':'fc 82%'},{'label':'tv','pct':18.0,'text':'tv 18%'}]; 
import sys
for st in ('classic','capsule','hairline'):
    out=styles.render(st, msgs_pct=42, weekly_pct=18, reset_5h='3h', reset_7d='5d', model='Opus 4.7', cache_age_text='5m', use_color=False, search_credits=E)
    first,_,rest=out.partition(chr(10))
    assert 'fc' in rest and 'fc' not in first, (st, repr(out))
    if st=='classic': assert 'cache 5m' in first and 'cache' not in rest, (st, repr(out))
    empty=styles.render(st, msgs_pct=42, weekly_pct=18, reset_5h='3h', reset_7d='5d', model='Opus 4.7', use_color=False, search_credits=[])
    assert 'fc' not in empty and chr(10) not in empty, (st, repr(empty))
print('OK all three styles: fc/tv on own row, cache on line 1, empty renders nothing')"</automated>
  </verify>
  <done>All three styles place fc/tv on a second row below the primary line; classic keeps `cache 5m` on the first line; empty search_credits yields a single line with no fc/tv and no stray newline.</done>
</task>

<task type="auto">
  <name>Task 2: Update + extend render tests, then run full suite and visually verify preview</name>
  <files>tests/test_provider_usage_render.py</files>
  <action>
Update tests/test_provider_usage_render.py to assert the NEW row placement and guard the cache interaction. Keep all existing newline-agnostic substring assertions (they still hold) and add placement checks.

1. The three classic battery tests call `progress.format_status_line(...)` directly, which no longer renders fc/tv. Convert each to call `styles.render_classic(...)` — the real production path — mapping params: `msgs_pct`, `weekly_pct`, `reset_5h`, `reset_7d`, `model`, plus `no_quota=True` (test_classic_search_credit_battery_no_quota with `ctx_pct=0`), `quota_stale=True` (test_classic_search_credit_battery_stale_mode), or plain quota mode (test_classic_search_credit_battery_quota_mode), all with `use_color=False, search_credits=_ENTRIES`. Keep the existing `"fc[" in out` / `"82%"` / `"tv[" ` / `"18%"` assertions. ADD a placement assertion in each: `first, _, rest = out.partition("\n"); assert "fc[" in rest and "fc[" not in first` (the primary quota text precedes the newline; fc/tv follow it).
2. Convert the two "renders nothing" classic tests (test_classic_empty_search_credits_renders_nothing, test_classic_none_search_credits_renders_nothing) to `styles.render_classic(...)` with `search_credits=[]` / `search_credits=None`; assert no `"fc"`/`"tv"` AND `"\n" not in out` (single line — proves no stray blank row when nothing else is multi-line).
3. ADD a new test `test_classic_cache_age_stays_on_quota_line` guarding the critical interaction: `styles.render_classic(msgs_pct=42, weekly_pct=18, reset_5h="3h", reset_7d="5d", model="Opus 4.7", cache_age_text="5m", use_color=False, search_credits=_ENTRIES)`; split on the first `"\n"`; assert `"cache 5m"` is in the first (primary) line, `"fc["` is in the second, and `"cache"` is NOT in the second line.
4. Extend the capsule and hairline chip tests (test_capsule_search_credit_chip, test_hairline_search_credit_chip): keep `"fc 82%" in out` / `"tv 18%" in out`, and ADD `first, _, rest = out.partition("\n"); assert "fc 82%" in rest and "fc 82%" not in first` so the chips are proven to sit on the row below the primary pills/segments.
5. Run the targeted file, then the full suite. Note: STATE.md records one pre-existing, unrelated `version_sync` failure that is deferred — that single known failure is acceptable; no OTHER test may regress. In particular confirm the newline-agnostic core.main()/daemon-env tests still pass: test_provider_usage_render.py::test_search_credits_sourced_from_os_environ_not_session_env (asserts `"fc[" in out` through the full render) and tests/test_search_credits_daemon_env.py (asserts `"fc[" in out` at ~line 377) — both remain green because render_classic emits `fc[` somewhere in the combined multi-line output.
6. Visual confirmation: render each style via `cs preview` and confirm fc/tv sit on their own row directly under the primary quota bar, with the demo cache-age (`3m24s`) staying on the quota line. The demo data (preview.py `_demo_data`) already carries `cache_age_text="3m24s"` plus fc/tv entries, so classic exercises the exact cache-interaction path.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_provider_usage_render.py -q && .venv/bin/python -m pytest -q -k "search_credit or provider_usage or daemon_env" && .venv/bin/cs preview --no-color --style classic --theme graphite | grep -q "fc\[" && echo "PREVIEW-CLASSIC-HAS-FC-ROW"</automated>
    <human-check>Run `.venv/bin/cs preview --no-color --style classic --theme graphite`, then `--style capsule` and `--style hairline`. Confirm for each: (a) fc/tv appear on their OWN line directly below the primary quota line, in that style's idiom; (b) the demo `cache 3m24s` text stays on the quota line, NOT on the fc/tv line; (c) no stray blank line.</human-check>
  </verify>
  <done>tests/test_provider_usage_render.py passes with the new placement + cache-interaction assertions; the full suite is green except the one pre-existing deferred version_sync failure; `cs preview` shows fc/tv on their own row under the quota bar in all three styles with cache-age on the quota line.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_provider_usage_render.py -q` — all render tests pass, including new placement and cache-interaction assertions.
- `.venv/bin/python -m pytest -q` — full suite green except the single known pre-existing `version_sync` failure (deferred, unrelated to this change).
- `cs preview --no-color --style {classic,capsule,hairline} --theme graphite` — fc/tv on their own row below the quota line; cache-age on the quota line; no blank line when credits are absent.
</verification>

<success_criteria>
- fc/tv search-credit bars render on a dedicated row directly below the primary quota line in classic, capsule, and hairline.
- Classic cache-age text remains on the primary line (never on the fc/tv row).
- Empty/None search_credits renders no extra line and no trailing blank-line artifact in any mode or style.
- Each style preserves its own idiom on the new row; no-color output carries no stray ANSI.
- No test regressions beyond the pre-existing deferred version_sync failure.
</success_criteria>

<output>
Create `.planning/quick/260715-ux9-move-fc-tv-search-credit-bars-onto-their/260715-ux9-01-SUMMARY.md` when done.
</output>
