---
phase: 12-per-account-rate-limit-store-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_account_rate_isolation.py
autonomous: true
requirements: [R1, R2, R3, R4, R5]
must_haves:
  truths:
    - "tests/test_account_rate_isolation.py exists and all 12 named tests are collectable (pytest --collect-only exits 0)."
    - "The suite encodes R1 (transcript beats $HOME/.claude.json), R4 (both store paths carry the uuid), R5a (unresolvable → legacy unsuffixed), R5b (named dir w/o own .claude.json never borrows $HOME uuid), the identity edge (same uuid two dirs → same store), and the sentinel 3-way branch."
    - "The suite encodes the two landmines (regime_changed_at reads the per-account path from projection(); _projection_result_key differs per account) and the three prohibitions (only accountUuid read; no destructive fs op on existing store files; no network/subprocess import)."
    - "test_two_accounts_share_5h_reset_render_own_values is a true core.main() end-to-end reproduction of the live 2026-07-16 collision (5h resets_at 1784191800; used 100 vs 50) — RED against current code, the phase's FAILS-pre-fix / PASSES-post-fix acceptance proof (R3)."
  artifacts:
    - "tests/test_account_rate_isolation.py — 12 tests + local helpers _write_claude_json / _payload_with_limits / _write_config"
  key_links:
    - "test module reuses tests.test_import_perf._list_imports_for for the no-network/subprocess prohibition (tests/ has __init__.py so the cross-test import works)"
    - "helpers copy the exact shape of tests/test_account.py (_write_claude_json) and tests/test_core_projection.py (_payload_with_limits, _write_config)"
  assumptions:
    - "Edge 'lifecycle (/login account switch mid-session)' is DISMISSED (SPEC ## Edge Coverage): transcript_path is immutable for a session; a re-login spawns a NEW session with a new transcript, so per-session resolution stays consistent and no in-session switch test is authored. Surfaced explicitly, not silently dropped."
    - "This plan authors the tests only; they are expected RED until predict.py (12-02, unit/landmine/prohibition tests) and core.py (12-03, the e2e regression) land. Each task's automated verify is --collect-only (well-formedness), not a green run."
---

<objective>
Author the complete isolation test suite `tests/test_account_rate_isolation.py` FIRST (Nyquist
Wave 0), so the fix in 12-02/12-03 is written against a fixed, executable specification. The
suite reproduces the live 2026-07-16 cross-account 5h collision end-to-end and pins every
requirement (R1–R5), both RESEARCH landmines, and all three SPEC prohibitions with concrete
pytest node-ids.

Purpose: R3's real deliverable is a regression test that FAILS pre-fix and PASSES post-fix.
Writing the whole suite up front (RED) makes that proof structural, gives 12-02/12-03 exact
green targets, and guarantees the subtle sentinel / regime / result-cache landmines each have a
dedicated assertion the fix cannot silently skip.

Output: `tests/test_account_rate_isolation.py` with 12 tests + local fixture helpers, all
collectable, all RED against current code.
</objective>

<execution_context>
@$HOME/.claude-account2/gsd-core/workflows/execute-plan.md
@$HOME/.claude-account2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-per-account-rate-limit-store-isolation/12-SPEC.md
@.planning/phases/12-per-account-rate-limit-store-isolation/12-RESEARCH.md
@.planning/phases/12-per-account-rate-limit-store-isolation/12-VALIDATION.md
</context>

<artifacts>
## Artifacts this phase produces (whole-phase manifest — new symbols; exclude from drift checks)

Created across 12-01/12-02/12-03:
- `predict._UNSET` — module-level sentinel object (distinguishes "arg omitted" from explicit `None`)
- `predict._SESSION_ACCOUNT_CACHE` — new dedicated memo dict `{"sig": None, "id": None}` (separate from `_ACCOUNT_CACHE`)
- `predict._claude_json_path_for_keying(config_dir, home) -> Optional[Path]` — R5b no-home-borrow locator
- `predict._read_keyed_account_id(path) -> Optional[str]` — path-aware accountUuid reader, memoized on `(path, mtime_ns, size)`
- `predict.account_id(stdin=None, *, env=None, home=None) -> Optional[str]` — new per-session signature (was zero-arg)
- new params: `_account_path(base, aid=_UNSET)`, `_latest_path(aid=_UNSET)`, `_projection_path(aid=_UNSET)`,
  `reconcile_account(..., account_uuid=_UNSET)`, `forecast(..., account_uuid=_UNSET)`,
  `projection(..., account_uuid=_UNSET)`, `quota_cache_status(..., account_uuid=_UNSET)`,
  `_projection_result_key(u5, r5, u7, r7, account_uuid=_UNSET)`
- core.py: `_resolved_account_uuid` local (resolved once, guarded), threaded into the reconcile_account /
  projection / forecast / quota_cache_status call sites
- `tests/test_account_rate_isolation.py` — this plan's new test file (12 tests)
- Note: `regime_changed_at` KEEPS its `(path=None)` signature — `projection()` passes `path=_latest_path(account_uuid)` (no new param).

This plan (12-01) produces `tests/test_account_rate_isolation.py` and its helpers only.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Resolution + keying + store-path unit tests (R1, R4, R5a, R5b, identity edge, sentinel, no-secret prohibition)</name>
  <files>tests/test_account_rate_isolation.py</files>
  <read_first>
    - .planning/phases/12-per-account-rate-limit-store-isolation/12-RESEARCH.md — "## Validation Architecture" (the detailed test designs: test_account_id_resolves_from_transcript_over_home_json, test_named_dir_without_own_json_does_not_borrow_home, test_account_path_three_way_branch, test_keying_reader_reads_only_account_uuid) AND "## Common Pitfalls" (sentinel tri-state, R5b no-borrow).
    - tests/test_account.py — copy the exact shape of `_write_claude_json(path, email=?, uuid=?)` (lines ~29-40): writes a minimal `.claude.json` with an `oauthAccount` block.
    - tests/test_core_projection.py — copy `_payload_with_limits(...)` (line ~18) and `_write_config(tmp_path, **values)` (line ~30); these build the stdin payload + on-disk config used by the e2e test in Task 2.
    - tests/test_account_switch.py — the monkeypatch idioms: `predict._CLAUDE_JSON_PATH`, `predict._ACCOUNT_CACHE`, `predict._LATEST_PATH`, `predict._PROJECTION_PATH`, and the zero-arg `predict.account_id = lambda: "..."` pattern (lines 29-113) that the sentinel design must keep working.
    - src/claude_statusbar/account.py — `resolve_config_dir` (55), `_config_dir_from_transcript` (37), `_claude_json_path` (81) — the reused resolver + the home-fallback locator the keying locator must NOT copy.
    - src/claude_statusbar/predict.py — current `_read_account_id`/`account_id`/`_account_path`/`_latest_path`/`_projection_path` (72-116); the anchored `accountUuid` regex `rb'"accountUuid"\s*:\s*"([0-9a-fA-F-]{8,64})"'`.
  </read_first>
  <action>
    Create tests/test_account_rate_isolation.py. Add local helpers copied from the read_first files:
    `_write_claude_json(path, uuid=...)` (write parents, an oauthAccount block with accountUuid — for the
    no-secret test also include a decoy `accessToken` and `emailAddress`), `_payload_with_limits(session_id,
    used_5h, reset_5h, used_7d, reset_7d, transcript_path=...)`, and `_write_config(tmp_path, **values)`.
    Author these 7 tests against the target predict.py API (expected RED until 12-02):
    (1) test_account_id_resolves_from_transcript_over_home_json — R1: `_write_claude_json(home/.claude.json,
    uuid="wrong-home-uuid")` as a decoy, write `~/.claude-account1/.claude.json` with
    `e1605250-1111-2222-3333-444455556666`, build stdin `{"transcript_path": <acct1 projects .jsonl>}`, assert
    `predict.account_id(stdin, env={}, home=tmp_path)` == the account1 uuid.
    (2) test_store_paths_carry_session_uuid — R4: for a resolved uuid, assert both `predict._latest_path(aid)`
    and `predict._projection_path(aid)` filenames contain the uuid[:12] segment (monkeypatch `_LATEST_PATH`/
    `_PROJECTION_PATH` under tmp_path).
    (3) test_unresolvable_session_uses_legacy_path — R5a: empty stdin `{}`, `env={}`, no resolvable accountUuid →
    `predict.account_id({}, env={}, home=tmp_path)` is None AND `predict._latest_path(None)` == the legacy
    unsuffixed `_LATEST_PATH`.
    (4) test_named_dir_without_own_json_does_not_borrow_home — R5b (sharpest edge): write
    `home/.claude.json` (uuid "home-account-uuid"), create `~/.claude-accountX/` with NO `.claude.json`;
    assert `predict.account_id({}, env={"CLAUDE_CONFIG_DIR": <accountX>}, home=tmp_path)` is None (NOT the home
    uuid) AND the resulting store path is the legacy unsuffixed path.
    (5) test_same_account_two_dirs_shares_store — identity edge (no over-isolation): two different config dirs
    whose `.claude.json` hold the SAME uuid resolve to the SAME `_latest_path` filename.
    (6) test_account_path_three_way_branch — sentinel landmine: monkeypatch `predict.account_id` to a zero-arg
    `lambda: "legacy-uuid"`; assert `predict._account_path(base)` (omitted) == base with `.legacy-uuid` suffix;
    `predict._account_path(base, aid=None)` == base (unsuffixed); `predict._account_path(base, aid="new-uuid")`
    == base with `.new-uuid` suffix.
    (7) test_keying_reader_reads_only_account_uuid — prohibition (no secret): a `.claude.json` whose oauthAccount
    also has `accessToken`/`refreshToken`; assert the reader (`predict._read_keyed_account_id(path)` or
    `account_id({...}, ...)` end-to-end) returns only the uuid and the secret substring never appears in the result.
    Do NOT implement any predict.py change here — tests only.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_account_rate_isolation.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_account_rate_isolation.py --collect-only -q` exits 0 and lists all 7 tests from this task.
    - Running the 7 tests now (`-k "resolves_from_transcript or store_paths_carry or unresolvable_session or named_dir_without or same_account_two_dirs or account_path_three_way or keying_reader_reads_only"`) fails RED (target API absent) — expected; do not mark done on green.
    - Helpers `_write_claude_json`, `_payload_with_limits`, `_write_config` are defined in-file (no cross-file import except the Task-2 `_list_imports_for` reuse).
  </acceptance_criteria>
  <done>Seven resolution/keying/path tests plus fixture helpers are authored and collectable; each targets the exact node-id from 12-VALIDATION.md; all are RED against current predict.py.</done>
</task>

<task type="auto">
  <name>Task 2: Threading/landmine + prohibition-import + e2e regression tests (R2, R3, regime + result-cache landmines, no-network / no-destructive-fs prohibitions)</name>
  <files>tests/test_account_rate_isolation.py</files>
  <read_first>
    - .planning/phases/12-per-account-rate-limit-store-isolation/12-RESEARCH.md — "## Validation Architecture" detailed designs for test_two_accounts_share_5h_reset_render_own_values, test_predict_module_imports_no_network_or_subprocess, test_no_destructive_fs_ops_on_existing_stores; AND "## Common Pitfalls" Pitfall 2 (regime_changed_at inside projection) and the _projection_result_key note (line ~284).
    - src/claude_statusbar/predict.py — `projection()` (1166-1200: internal `regime_changed_at()` at 1185, `load/save_projection_store`, `_projection_result_key` at 1172), `reconcile_account` (349-538), `regime_changed_at` (338-346), and `_projection_result_key` (1081-1094).
    - src/claude_statusbar/core.py — `main()` entry, `parse_stdin_data` (33-102), `_effective_env` (1064-1066), the reconcile_account call at 1424, projection at 1500, forecast at 1517, quota_cache_status at 1572 — the e2e test drives all of this via core.main().
    - src/claude_statusbar/cache.py — `atomic_write_text` uses `os.replace(tmp, path)` on its OWN target (line ~34): the destructive-fs test must scope its assertion to the pre-existing stale file, NOT "no os.replace anywhere".
    - tests/test_import_perf.py — `_list_imports_for(module)` (line ~26): import it directly (`from tests.test_import_perf import _list_imports_for`) for the no-network prohibition.
  </read_first>
  <action>
    Append these 5 tests to tests/test_account_rate_isolation.py (expected RED until 12-02 for the three unit
    ones and until 12-03 for the e2e regression):
    (8) test_projection_threads_account_into_regime_check — regime landmine: monkeypatch `predict.regime_changed_at`
    to capture the `path` kwarg it receives; call `predict.projection(..., account_uuid=<uuid>)`; assert the
    captured path equals `predict._latest_path(<uuid>)` (the per-account reconcile store), NOT the legacy path.
    (9) test_projection_result_cache_keys_by_account — result-cache landmine: reset `predict._PROJECTION_RESULT_CACHE`,
    call `predict._projection_result_key(u5, r5, u7, r7, account_uuid="acct-a")` and `...account_uuid="acct-b")`;
    assert the two keys differ (the path segments differ), so the 1s result cache cannot return acct-a's tuple to acct-b.
    (10) test_predict_module_imports_no_network_or_subprocess — prohibition: `loaded = _list_imports_for("claude_statusbar.predict")`;
    assert `{"subprocess","socket","urllib","urllib.request"} & loaded` is empty.
    (11) test_no_destructive_fs_ops_on_existing_stores — prohibition (fix-forward): create a pre-existing
    `rate_latest.stale-uuid.json`; monkeypatch `os.unlink`/`os.replace`/`Path.rename` to append to a calls list;
    monkeypatch `predict._LATEST_PATH` under tmp_path; run `predict.reconcile_account(10.0, <future>, 8.0, <future>,
    now=..., account_uuid="new-account")`; assert NO recorded destructive call targets the pre-existing `stale`
    path specifically (the store's own atomic-write os.replace on its own target is allowed).
    (12) test_two_accounts_share_5h_reset_render_own_values — THE regression test (R3): monkeypatch HOME=tmp_path;
    `_write_config(tmp_path, show_forecast=False, show_projection=False)`; create `~/.claude-account1` and
    `~/.claude-account2`, each with its OWN `.claude.json` (uuids e1605250… and c87262ba…); create per-account
    transcript paths `<dir>/projects/-enc-/sidN.jsonl`; build payload1 (used_5h=100.0, resets_5h=1784191800,
    transcript=acct1) and payload2 (used_5h=50.0, resets_5h=1784191800 IDENTICAL, transcript=acct2); drive
    `core.main(use_color=False, _suppress_side_effects=True)` once per payload via monkeypatched `sys.stdin`;
    capture stdout each time; assert account1's render contains its own 100% and account2's contains its own 50%
    (NOT 100). Confirm the exact percentage substring against the render style during implementation (reuse a
    render-format helper from tests/test_forecast_render.py if the classic digit formatting is ambiguous).
    Do NOT implement any src/ change here — tests only.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_account_rate_isolation.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_account_rate_isolation.py --collect-only -q` exits 0 and lists all 12 tests (7 from Task 1 + 5 here).
    - `test_predict_module_imports_no_network_or_subprocess` imports `_list_imports_for` from tests.test_import_perf (no reinvented harness).
    - `test_no_destructive_fs_ops_on_existing_stores` asserts only that the pre-existing stale file is untouched (does NOT ban os.replace globally).
    - Running the full file now is RED (regression + landmine + prohibition targets absent) — expected; do not mark done on green.
  </acceptance_criteria>
  <done>All 12 tests are authored and collectable; the e2e regression reproduces the live 100-vs-50 collision through core.main(); landmine and prohibition tests use the exact node-ids and the reused import harness; suite is RED against current code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → filesystem (tmp_path) | Tests write synthetic `.claude.json` / store files under `tmp_path` only; no real user config touched. |
| test → real modules under test | Tests import predict/core/account but exercise them with monkeypatched HOME/env/stdin, never network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | test fixtures embedding a decoy `accessToken` | low | accept | The secret is a synthetic literal used only to assert the reader ignores it; never a real token, never persisted outside tmp_path. |
| T-12-02 | Tampering | monkeypatched os.unlink/os.replace/Path.rename | low | mitigate | Patches are function-scoped via pytest monkeypatch and auto-reverted at test teardown; they only record calls, never suppress real behavior beyond the test body. |
| T-12-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs — pytest/monkeypatch already in .venv (RESEARCH: Package Legitimacy Audit not applicable). No legitimacy gate required. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_account_rate_isolation.py --collect-only -q` exits 0 with all 12 tests listed.
- The file defines `_write_claude_json`, `_payload_with_limits`, `_write_config` locally and imports only `_list_imports_for` across test files.
- Expected state at end of this plan: the 12 tests are RED (implementation lands in 12-02/12-03). This is the intended Nyquist Wave 0 outcome, not a failure.
</verification>

<success_criteria>
- `tests/test_account_rate_isolation.py` exists with exactly the 12 node-ids from 12-VALIDATION.md, all collectable.
- Every requirement (R1–R5), both landmines, and all three prohibitions have a dedicated test.
- The e2e regression is a genuine core.main() reproduction of the live collision (RED now, GREEN after 12-03).
</success_criteria>

<output>
Create `.planning/phases/12-per-account-rate-limit-store-isolation/12-01-SUMMARY.md` when done.
</output>
